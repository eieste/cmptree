import helper
import components as cmp
from basecomponents import ElectricSignal as ES
import json
import plotly.express as px

#helper.show_tree_to_end(cmp.main_power_distribution, "electric", ES.POWER_IN)
#helper.show_tree_to_end(cmp.main_power_distribution, "electric", ES.I2C)
